[package]
name = "axelar_auth_weighted"
version = "1.0.0"
authors = ["John Doe <john@doe.org>"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
near-sdk = "4.0.0"
near-contract-tools = "0.7.1"
uint = { version = "0.9.3", default-features = false }
utils = { path = "../utils" }